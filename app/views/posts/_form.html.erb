<%= form_for(@post, :html => {:class => 'pure-form pure-form-aligned'}) do |f| %>
	<fieldset>
		<% if @post.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

			<ul>
				<% @post.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
		<% end %>

		<div class="pure-control-group">
			<%= f.label :title %>
			<%= f.text_field :title, :class => 'pure-input-1-2' %>
		</div>
		<div class="pure-control-group">
			<%= f.label :body %>
			<%= f.text_area :body, :class => 'pure-input-1-2', :rows =>'6' %>
		</div>
		<%= f.hidden_field :author_id, :as => :hidden, :value => current_author.id %>
		<%= f.hidden_field :date, :as => :hidden, :value => Date.today %>
		
		<!-- <%= hidden_field_tag :author_id, current_author.id %> -->
		<!-- <%= hidden_field_tag :date, Date.today %> -->
		<!-- <#= f.hidden_field(:author, :id) %> -->
		<!-- <input type="hidden" id="author_id" name="author_id" value="#{current_author.id}"> -->
		<!--<div class="field">
			<#= f.label :date %><br>
			<#= f.date_select :date %>
			</div>
			<div class="field">
			<#= f.label :author_id %><br>
			<#= f.text_field :author_id %>
			</div>-->
			<div class="pure-controls">
				<%= f.submit :class => 'pure-button button-uh' %>
			</div>
	</fieldset>
<% end %>
